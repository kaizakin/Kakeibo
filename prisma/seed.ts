// Seed script has been intentionally disabled.
// Database is now managed through the app UI (import CSV) rather than preset seed data.
// To restore seeding, uncomment the seed logic or create a new seed script.
